# {{ service_name }}

A {{ language }} service scaffolded with [golden-path-scaffolder](https://github.com/tenalisriharsha/golden-path-scaffolder).

## Run locally

```bash
go run ./cmd/server
```

Health check: `GET {{ health_path }}` on port {{ port }}.
